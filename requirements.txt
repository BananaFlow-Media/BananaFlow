# ── Core downloading engine ──────────────────────────────────────────────────
# requirements.txt is the reproducible application/release install path used by
# the Windows and macOS build workflows. Keep yt-dlp on the exact reviewed
# exact release here; ordinary source installs through `pip install .` use
# pyproject.toml's safe >=2026.8.19 compatibility floor instead.
# The [default] extra installs yt-dlp's matching EJS dependency, so do not list
# yt-dlp-ejs independently.
yt-dlp[default]==2026.8.30.232658.dev0

# PO Token Provider plugin: not a source-tree runtime requirement, but
# the public Windows build pins and stages bgutil-ytdlp-pot-provider
# plus its upstream Deno script backend via packaging/stage_pot_provider.py.
# The provider is GPL v3-compatible with BananaFlow's GPL-3.0-or-later release.
# Source/venv developers can still opt in with:
#     pip install "bgutil-ytdlp-pot-provider==1.3.2"
# BananaFlow passes yt-dlp the provider's official server_home argument when
# the bundled backend is present; it never generates, stores, or injects
# PO Tokens itself. See THIRD_PARTY_NOTICES.md for license notices.

# ── Audio & Video post-processing ─────────────────────────────────────────────
ffmpeg-python>=0.2.0       # Python bindings for media probing
pyloudnorm>=0.2.0          # ReplayGain fallback (optional; rsgain binary preferred)
soundfile>=0.14.0          # Required by pyloudnorm

# ── Metadata & tag writing ────────────────────────────────────────────────────
mutagen>=1.48.1            # Read/write metadata (MP3, MP4, FLAC, etc.)
syncedlyrics>=1.0.1        # Lyrics (disabled by default)
send2trash>=2.1.0          # Recycle Bin deletes for the Tag Editor (files & folders)

# ── GUI Framework (PySide6 + Fluent Widgets) ──────────────────────────────────
PySide6>=6.11.2                        # Latest stable Qt6 bindings
PySide6-Fluent-Widgets>=1.11.3         # Adjusted to available versions

# ── Networking & Scraping (The new "Universal" engine) ───────────────────────
requests>=2.34.2           # Standard HTTP for thumbnails & updates
httpx>=0.28.1              # Async HTTP - Crucial for the new page scraper
beautifulsoup4>=4.15.0     # HTML parsing for deep link extraction
lxml>=6.1.3                # Faster parser backend for BeautifulSoup
ytmusicapi>=1.12.2         # YouTube Music metadata and search

# ── Utilities ─────────────────────────────────────────────────────────────────
Pillow>=12.3.0             # Image handling; 12.3.0 includes current security fixes
python-dotenv>=1.2.3       # Environment variables (.env)
certifi>=2026.7.22         # Ensure SSL certificates are up to date
keyboard>=0.13.5; sys_platform == "win32"   # Global hotkeys — Windows only (needs root on macOS/Linux); app degrades gracefully
playwright>=1.62.0         # Headless browser for scraping platform logic
