# Gitleaks false-positive allowlist — exact-fingerprint suppressions only.
#
# Each line is `commit:file:rule-id:start-line` for ONE confirmed-benign
# finding, verified by hand before being added here. Never add a broad
# rule/path exclusion to silence a real category of finding — only exact
# fingerprints for findings that were individually checked and are not
# secrets.

# generic-api-key false positive: the matched text is "thumbnail_url=" in a
# test fixture (tests/test_pause_resume_continuation.py:158) building a
# DownloadRequest with thumbnail_url="http://thumb" — a plain placeholder
# URL, not a credential. Verified locally with gitleaks 8.30.1 against
# .gitleaks.toml; Secret field in the finding is literally "thumbnail_url=".
ab5df86c3220995c7ecf21cab75b5940752e64bd:tests/test_pause_resume_continuation.py:generic-api-key:158
